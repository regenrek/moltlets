docs:
  - path: docs/index.md
    when: start here
    summary: entrypoint; repo model; operator vs bot split; threat model notes
  - path: docs/overview.md
    when: mental model, repo layout, lifecycle
    summary: git-safe infra; encrypted secrets in /secrets; runtime in .clawdlets; bootstrap -> lockdown -> ops
  - path: docs/golden-path.md
    when: first-class workflow
    summary: bootstrap once, then push -> CI build+manifest -> deploy by store path (cache-only)
  - path: docs/quickstart.md
    when: first run
    summary: minimal command sequence to deploy one host
  - path: docs/poc.md
    when: definition of done
    summary: acceptance criteria for first host
  - path: docs/cli.md
    when: you need the right command
    summary: clawdlets and just cookbook
  - path: docs/config.md
    when: editing fleet/clawdlets.json
    summary: schema/fields/example
  - path: docs/agent-config.md
    when: routing, skills, workspaces
    summary: fleet.nix knobs; per-bot routing/skills/codex/github app
  - path: docs/runtime-layout.md
    when: paths, state dirs, credentials
    summary: canonical runtime paths on the host
  - path: docs/install.md
    when: prerequisites on workstation/server
    summary: required tools and setup notes
  - path: docs/deploy.md
    when: updating a host
    summary: deploy patterns; pin rev; rollback mental model
  - path: docs/image-based-provisioning.md
    when: image-based provisioning
    summary: phase-4 plan to reach cattle (image builds + upload + secrets injection)
  - path: docs/cattle.md
    when: ephemeral agents, burst capacity
    summary: cattle mode (spawn/list/reap) and the task VM lifecycle
  - path: docs/orchestrator.md
    when: bot-facing queue, running jobs from Discord
    summary: clf + orchestrator (job queue, cattle spawn handlers, local unix socket)
  - path: docs/concepts/index.md
    when: concepts hub
    summary: background concepts for clawdlets/clf
  - path: docs/concepts/queue.md
    when: how jobs run
    summary: clf queue semantics (leases/retry/idempotency) + security boundary
  - path: docs/secrets.md
    when: tokens/keys/sops/age
    summary: local keys; encrypted host secrets; host secret path default
  - path: docs/security.md
    when: threat model, hardening
    summary: secrets out of store; lockdown verification checks
  - path: docs/ops-invariants.md
    when: rules of engagement
    summary: deploy-only policy; no manual host edits
  - path: docs/operations.md
    when: day-2 ops
    summary: status/logs/restart/deploy patterns
  - path: docs/runbook.md
    when: day 0/day 2 checklist
    summary: bootstrap + routine ops + optional sync/snapshots
  - path: docs/troubleshooting.md
    when: broken deploy
    summary: known errors and fixes
  - path: docs/publicing.md
    when: OSS prep
    summary: keep instance data out of git; clean export checklist
  - path: docs/upstream.md
    when: tracking nix-clawdbot updates
    summary: how to keep fork aligned with upstream changes
