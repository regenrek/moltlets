---
title: Passthrough
description: Advanced OpenClaw keys via hosts.<host>.gateways.<gatewayId>.openclaw.
---

Use passthrough when you need an OpenClaw key that is not exposed as a first-class
Clawlets surface.

Where it lives:

- `hosts.<host>.gateways.<gatewayId>.openclaw` (raw object merged into the OpenClaw root config)

<Callout type="warning" title="Do not put typed surfaces under openclaw.*">
  Do not set <code>openclaw.channels</code>/<code>openclaw.agents</code>/<code>openclaw.hooks</code>/<code>openclaw.skills</code>/<code>openclaw.plugins</code>.
  Use the typed surfaces on the gateway instead.
</Callout>

## Example: set logging defaults

```json
{
  "hosts": {
    "openclaw-fleet-host": {
      "gatewaysOrder": ["maren"],
      "gateways": {
        "maren": {
          "openclaw": {
            "logging": {
              "level": "info",
              "redactSensitive": "tools"
            }
          }
        }
      }
    }
  }
}
```

## Example: customize message formatting

```json
{
  "hosts": {
    "openclaw-fleet-host": {
      "gatewaysOrder": ["maren"],
      "gateways": {
        "maren": {
          "openclaw": {
            "messages": {
              "responsePrefix": ""
            }
          }
        }
      }
    }
  }
}
```

## How it merges

Passthrough merges into the gateway's OpenClaw root config, then Clawlets invariants win
last (gateway bind/port/auth and workspace policy).

See:

- [Typed config surfaces](/configuration/typed-surfaces)
