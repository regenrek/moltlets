---
title: Agents
description: Model defaults, fallbacks, and concurrency.
---

This page shows common agent defaults. These live under:

- `fleet.gateways.<bot>.agents.defaults`

## Model default (per bot)

```json
{
  "fleet": {
    "gateways": {
      "maren": {
        "agents": {
          "defaults": {
            "model": {
              "primary": "zai/glm-4.7",
              "fallbacks": ["anthropic/claude-opus-4-5"]
            },
            "thinkingDefault": "low",
            "maxConcurrent": 4
          }
        }
      }
    }
  }
}
```

Notes:

- `thinkingDefault` is optional; use it to set a consistent baseline.
- `maxConcurrent` is per-bot concurrency, not a global host limit.

## Host default (applies to all bots unless overridden)

You can set a host-level default model and then override per bot if needed:

```json
{
  "hosts": {
    "openclaw-fleet-host": {
      "agentModelPrimary": "zai/glm-4.7"
    }
  }
}
```

## Workspace + bootstrap policy (derived)

Clawlets pins workspace paths and bootstrap behavior at deploy time (Nix invariants).
You normally should not override these via passthrough.

See:

- [Typed config surfaces](/configuration/typed-surfaces)
- [Agent config](/configuration/agent-config)

