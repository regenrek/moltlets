---
title: CLI overview
description: Common commands and workflows.
---

The CLI is how you automate and script Clawlets. Most users should start in the
dashboard and use the CLI when they need repeatable ops, CI integration, or advanced
flows. In day-to-day work, the dashboard is the primary interface; the CLI becomes
important as soon as you want reproducible runs or bulk changes.

Canonical source: `clawlets --help`.

## CLI user quick path

1. Install CLI:

```bash
npm install -g clawlets
clawlets --help
```

2. If you want local dashboard + Convex bootstrap from CLI:

```bash
clawlets quickstart
```

For non-interactive automation:

```bash
clawlets quickstart --no-confirm --ui none --json
```

3. Continue with [Command reference](/cli/commands).

## Core workflow (project repo)

A typical flow looks like this:

1) scaffold a repo
2) choose provider + configure host Day 0 fields
3) wire secrets
4) bootstrap Day 0 infra
5) apply updates
6) iterate OpenClaw Day X config

Example command sequence:

- scaffold: `clawlets project init --dir ./clawlets-myproject`
- add SSH keys (fleet scope): `clawlets host set --add-ssh-key-file ~/.ssh/id_ed25519.pub`
- choose provider first: `clawlets host set --host <host> --provider hetzner`
- add host settings:
  - `clawlets host set --host <host> --admin-cidr <cidr> --ssh-pubkey-file ~/.ssh/id_ed25519.pub`
  - provider fields (Hetzner): `clawlets host set --host <host> --server-type cx43 --hetzner-location nbg1`
  - provider fields (AWS): `clawlets host set --host <host> --aws-region us-east-1 --aws-instance-type t3.large --aws-ami-id ami-... --aws-use-default-vpc true`
- add gateways: `clawlets gateway add --host <host> --gateway <gatewayId>`
- wire secrets:
  - map env vars in `fleet.secretEnv` / per‑gateway overrides
  - or autowire: `clawlets config wire-secrets --write`
- init deploy creds: `clawlets env init` (edit `.clawlets/env`; set provider credentials: `HCLOUD_TOKEN` for Hetzner or AWS keys for AWS)
- init secrets: `clawlets secrets init` (TTY prompt) or `clawlets secrets init --from-json <file>`
- preflight: `clawlets doctor --scope bootstrap`
- bootstrap: `clawlets bootstrap` (or `--mode image`)
- set ops target: `clawlets host set --host <host> --target-host admin@<ipv4-or-magicdns>`
- apply updates: `clawlets server update apply --host <host>`
- lockdown (tailnet‑only SSH):
  - `clawlets host set --host <host> --ssh-exposure tailnet`
  - `clawlets lockdown`

Day 0 scope: provider bootstrap and host connectivity (`bootstrap`, `infra`, `lockdown`).
Day X scope: OpenClaw gateway config, channels, skills, and ongoing updates.

## Server ops (SSH)

Once a host is up, most operational actions flow through these commands. They are
safe to run repeatedly and are the same actions the dashboard triggers internally.

- status: `clawlets server status --host <host>`
- audit: `clawlets server audit --host <host>`
- logs: `clawlets server logs --host <host> --unit openclaw-<gatewayId>.service --since 10m --follow`
- restart: `clawlets server restart --host <host> --unit openclaw-<gatewayId>.service`
- updater: `clawlets server update apply|status|logs --host <host>`

## Plugins (experimental)

External CLF/cattle plugins are experimental and not part of the core supported path.
See [Plugins overview](/plugins).

## Justfile shortcuts

Run `just --list`.
